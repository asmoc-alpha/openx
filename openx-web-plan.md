# OpenX Web 应用实现计划（基于「Agent Web App - 三栏布局」设计稿）

> 状态：待审阅  
> 目标：把现有 `openx serve` 的 Web 端从「会话 + 聊天 + 产物」三栏，升级为设计稿中的「智能体 / 聊天 / 任务面板」三栏布局，同时复用并扩展现有后端能力。

---

## 1. 设计稿要点与现有实现对照

### 1.1 设计稿结构

| 区域     | 设计稿内容                                          | 现有 openx serve 对应                         |
| ------ | ---------------------------------------------- | ----------------------------------------- |
| **左栏** | Atlas Agent 品牌、新建对话、全局搜索、智能体列表、最近对话、用户资料/Token | 已有「会话」侧栏，缺智能体列表、用户资料、全局搜索                 |
| **中栏** | 对话标题面包屑、消息流（用户 + 助手卡片含标签/工具调用/附件）、底部输入区        | 已有基础聊天，缺面包屑、标签、工具调用卡片化、附件展示               |
| **右栏** | 任务面板（任务流 / 上下文 / 产物）、协作流程进度、并行 Agent 状态        | 当前为「本次改动 + 文件树」产物面板，缺任务/工作流可视化和多 Agent 监控 |

### 1.2 可复用的现有能力

- `openx/app/serve/server.py`：`aiohttp` 服务、`/ws`、`/api/*`
- `openx/app/serve/session.py`：WebSocket 广播、回合队列、attach 快照
- `openx/app/serve/bridge.py`：权限 / `ask_user` / `plan_request` 桥
- `openx/app/serve/api.py`：会话、文件、产物、模型、MCP、Skill、Plugin 管理 API
- `openx/orchestration/workflow.py` + `fleet.py` + `subagent.py`：多 Agent 工作流、并行子代理、子代理规格
- `openx/orchestration/tasks.py`：后台任务注册表
- 现有 WS 协议：`system/init`、`history`、`user_message`、`text_delta`、`thinking_delta`、`tool_use`、`tool_result`、`result`、`interrupted`、`permission_request`、`ask_user`、`plan_request`、`panels`

### 1.3 需要新增的能力

- 智能体列表 API 与注册表可视化
- 任务/工作流进度事件流（右栏「任务面板」的数据源）
- 并行 Agent 运行时快照（复用 `FleetMonitor`）
- Token/用量/用户资料展示（读取已有 usage 统计）
- 消息卡片增强（标签、附件、工具调用组）

---

## 2. 关键决策（需要你在审阅时拍板）

| # | 决策项           | 方案 A（推荐）                                                                   | 方案 B                       |
| - | ------------- | -------------------------------------------------------------------------- | -------------------------- |
| 1 | **品牌**        | 保留 OpenX 品牌，吸收设计稿的视觉语言（三栏、暗色、卡片、辉光状态点）                                     | 完全复刻 Atlas Agent 品牌        |
| 2 | **技术栈**       | 继续 **vanilla JS + CSS**，无构建步骤，保持 `openx serve`「零依赖、无 CDN」的部署体验             | 引入 Vite/React/Vue 等现代前端构建链 |
| 3 | **右栏定位**      | 改为「任务面板」三标签：**任务流** / **上下文** / **产物**；产物/文件从当前右栏移入「产物」标签                  | 保留现有「产物 + 文件树」右栏，新增独立任务抽屉  |
| 4 | **智能体列表数据来源** | 读取 `openx/orchestration/subagent.py` 加载的 builtin + `.openx/agents/*.md` 规格 | 仅展示硬编码示例，后端暂不改造            |
| 5 | **任务流数据来源**   | 复用 `FleetMonitor` + `WorkflowStats`，新增 WebSocket 事件广播                      | 先做前端 mock 数据，后端后补          |
| 6 | **实现节奏**      | 分 5 个阶段，每阶段可独立验收、可回滚                                                       | 一次性大改版                     |

> 默认按「方案 A」排期。如果你倾向其他方案，我会在下一阶段调整。



---

## 3. 分阶段实施计划

### Phase 0：基础与脚手架（约 1–2 天）

**目标**：确定方向、搭建新前端骨架、保证现有测试不挂。

1. **目录与构建**
   - 在 `openx/app/serve/web/` 下逐步重构（不另起 `web_v2`，减少迁移成本）。
   - 保留现有 `index.html` 作为 fallback，新增 `index.html` 直接替换；或在同一文件内按模块拆分。
   - 拆分静态文件：`layout.css`、`components.css`、`chat.css`、`task-panel.css`、`app.js`、`chat.js`、`task-panel.js`、`agents.js`、`api.js`、`ws.js`、`markdown.js`。
2. **设计令牌**
   - 在 `:root` 定义设计稿色板：深色背景阶梯、强调紫/蓝、状态绿/红/琥珀、圆角、字体。
   - 建立 spacing、shadow、animation 变量体系。
3. **向后兼容检查**
   - 确认现有 WS 协议字段不变；新增事件以 `task_*` / `agent_*` 前缀扩展。
   - 跑通 `tests/serve/` 与 `tests/app/` 相关用例，保证不破坏现有 Web 端行为。
4. **产出**
   - 新目录结构落定。
   - `docs/openx-serve-ui-v2.md` 草稿：组件清单 + 协议扩展说明。

### Phase 1：布局外壳（约 2–3 天）

**目标**：先把三栏视觉和交互骨架搭出来，数据用静态/mock。

1. **三栏布局**
   - 左栏固定宽 260px、中栏弹性、右栏固定宽 340px（参考设计稿比例）。
   - 拖拽分隔条保留并美化（细线 + 悬停高亮）。
   - 移动端：中栏全屏，左右栏通过顶部按钮切换抽屉。
2. **顶栏**
   - 左侧：OpenX Logo + 副标题「智能助手工作台」。
   - 中部：当前会话标题面包屑（`研究助手 · 协作模式 > 任务标题`）。
   - 右侧：分享 / 导出 / 更多、模型选择、设置、连接状态。
3. **左栏组件（静态）**
   - 「新建对话」主按钮。
   - 全局搜索框。
   - 「智能体」分组：标题 + 「+」按钮；列表项含图标、名称、未读/数量徽标、在线状态点。
   - 「最近对话」分组：标题 + 数量；列表项含标题、标签（代码/写作/研究…）、时间。
   - 底部用户资料：头像、名称、PRO 徽标、Token 用量进度条。
4. **中栏聊天区（静态）**
   - 消息容器、滚动行为。
   - 用户消息气泡样式。
   - 助手消息卡片：顶部标签行（头像、Agent 名、模型/增强标签、时间/Token/耗时）、正文区、工具调用折叠卡片、附件标签。
   - 输入区：多行文本框、附件/代码/图片/语音按钮、发送按钮、快捷键提示。
5. **右栏任务面板（静态）**
   - 头部：标题「任务面板」+ 「实时」徽标 + 设置/关闭。
   - 标签页：任务流 / 上下文 / 产物。
   - 「任务流」标签：当前任务卡片（步骤、进度、预计时间）+ 协作流程步骤列表（完成/执行中/等待）。
   - 「并行 Agent」区：Agent 卡片（图标、名称、职责、Token/耗时、运行状态）。
   - 「上下文」「产物」标签：占位结构。
6. **产出**
   - 可 `openx serve` 启动并看到与设计稿一致的三栏静态页面。
   - 无后端联动，数据为 mock。

### Phase 2：后端 API 与协议扩展（约 3–4 天）

**目标**：让前端有真实数据可接。

1. **智能体相关 API**
   - `GET /api/agents`：返回 builtin + 项目级 `.openx/agents/*.md` 子代理列表（名称、描述、类型、触发词、图标颜色）。
   - `POST /api/agents/{name}/run`（可选）：从侧边栏直接触发一个子代理任务；先不做，留接口占位。
2. **用户/用量 API**
   - `GET /api/usage`：当前会话/今日/总计 Token、费用估算、会话数。
   - `GET /api/me`：用户显示名、计划（PRO/Free）、头像首字母。
3. **会话列表增强**
   - `GET /api/sessions` 增加 `agent_type` / `tags` 字段，用于左栏「最近对话」的图标和分类。
4. **WebSocket 协议扩展（新增下行事件）**
   - `task_started`：`{task_id, title, total_steps, agents}`
   - `task_updated`：`{task_id, current_step, progress_percent, elapsed_ms, estimated_remaining_ms, steps:[{name, status, duration_ms, detail}]}`
   - `task_completed`：`{task_id, duration_ms, usage}`
   - `agent_progress`：`{task_id, agent_id, name, role, status, tokens, output_tokens, elapsed_ms}`
   - `context_changed`：当前上下文文件/引用快照（右栏「上下文」标签）
   - 保持旧事件不变，前端按 `ev.type` 分发。
5. **服务端集成**
   - 在 `ServeSession` 中接入 `FleetMonitor` 快照：子代理启动/更新/完成事件化并广播。
   - 在 `workflow_tool.py` 中注入 `phase()` / `log()` 的 Web 广播钩子。
   - 在 `subagent_tool.py` / `task_tools.py` 中广播 `agent_progress`。
6. **产物与上下文**
   - 右栏「产物」标签直接复用现有 `/api/artifacts` 与 `/api/files`。
   - 新增 `/api/context`：返回当前会话引用的文件路径、工具调用摘要（用于「上下文」标签）。
7. **产出**
   - 后端新增/扩展的 API 与 WS 事件可用。
   - 单元测试覆盖新增端点与事件投影。

### Phase 3：前端组件与数据流（约 4–5 天）

**目标**：把 Phase 1 的静态组件接起来，实现完整交互。

1. **状态管理**
   - 用纯函数 reducer：`state = { session, messages, tasks, agents, context, artifacts, permissions, settings, ui }`。
   - 事件总线：`ws.js` 负责重连、心跳、上行；`events.js` 负责下行事件到 state 的映射。
2. **左栏**
   - 从 `/api/agents` 加载智能体列表，点击可切换当前 Agent/模式。
   - 从 `/api/sessions` 加载最近对话，搜索过滤，点击切换会话。
   - 用户资料与 Token 用量从 `/api/me`、`/api/usage` 加载。
3. **中栏聊天**
   - 接入 `history`、`user_message`、`text_delta`、`thinking_delta`、`tool_use`、`tool_result`。
   - 助手卡片动态更新：标签行显示模型、耗时、Token；工具调用卡片化（可展开/折叠、运行中脉冲）。
   - 附件渲染：文件标签点击预览。
   - 输入区：快捷键 Enter 发送 / Shift+Enter 换行、@ 提及智能体（可选）、附件选择。
4. **右栏任务面板**
   - 接入 `task_started` / `task_updated` / `task_completed` / `agent_progress`。
   - 任务流步骤自动推进、进度条动画、预计时间倒计时。
   - 并行 Agent 卡片实时更新 Token 和耗时。
   - 「上下文」标签展示当前引用文件与摘要。
   - 「产物」标签展示本次改动文件列表与文件树（复用现有逻辑）。
5. **弹窗**
   - 权限弹窗、ask_user 弹窗、计划审批弹窗按现有协议复用，视觉升级。
   - 设置弹窗整合模型、MCP、Skill、Plugin 管理（复用现有 settings.js 逻辑）。
6. **产出**
   - 完整可交互的 Web 端，能真实聊天、看到任务进度、管理会话。

### Phase 4：集成测试与打磨（约 2–3 天）

**目标**：稳定、可发布。

1. **端到端测试**
   - 用 `aiohttp.test_utils` 起真实服务，覆盖：连接、发送消息、接收流式回复、权限弹窗、任务面板更新、会话切换。
   - 前端用 Playwright（如环境允许）或 jsdom 做关键组件交互测试。
2. **视觉与体验打磨**
   - 暗色/浅色主题切换（设计稿为暗色，先保证暗色完美）。
   - 空状态、加载骨架、错误提示、断线重连提示。
   - 拖拽分隔条记忆宽度到 `localStorage`。
   - 键盘快捷键文档。
3. **性能**
   - 大消息列表虚拟滚动（如会话消息 > 200 条）。
   - 任务面板高频率更新节流（`task_updated` 合并为 ~4Hz 渲染）。
4. **文档**
   - 更新 `docs/openx-serve.md`：新增 UI 截图、协议扩展表、API 列表。
   - 更新 `README.md` Web 端描述。
5. **产出**
   - 测试通过。
   - 文档更新。
   - 准备合并。

---

## 4. 验收标准

| 维度     | 标准                                                                         |
| ------ | -------------------------------------------------------------------------- |
| **功能** | 左栏智能体/最近对话/搜索可交互；中栏可聊天并看到流式回复、工具调用、思考过程；右栏任务面板能跟随工作流/子代理实时更新；权限/提问/计划弹窗正常。 |
| **协议** | 现有 WS 事件与 API 保持向后兼容；新增事件有文档和测试。                                           |
| **视觉** | 与设计稿三栏布局、间距、配色、状态点、卡片风格基本一致；暗色主题完整。                                        |
| **性能** | 首屏加载 < 1s（本地）；100 条消息内滚动流畅；任务面板更新不卡顿。                                      |
| **测试** | 新增后端 API 测试、WS 事件测试；现有 `tests/serve/` 不挂。                                  |
| **部署** | `pip install 'openx[web]'` + `openx serve` 直接可用，无额外构建。                     |

---

## 5. 风险与依赖

| 风险                                                          | 影响 | 缓解措施                                        |
| ----------------------------------------------------------- | -- | ------------------------------------------- |
| `FleetMonitor` / `WorkflowStats` 当前面向 TUI，改造成事件广播可能牵涉锁/线程模型 | 中  | 只读快照广播，生产者在事件循环内，消费者是 WS 发送任务，与 TUI 渲染线程隔离。 |
| 设计稿右栏「任务面板」需要实时多 Agent 数据，若当前工作流未启用则面板为空                    | 低  | 未运行工作流时显示空状态提示；普通单 Agent 聊天也能显示当前 Agent 状态。 |
| 静态前端文件增多，维护成本上升                                             | 中  | 模块化拆分 + 文档；必要时后续阶段可引入轻量构建链（但本计划保持无构建）。      |
| 大仓库文件树渲染慢                                                   | 低  | 保持现有 `/api/files` 分页/截断策略；右栏产物标签按需加载。       |

---

## 6. 建议的下一步

1. 你确认或调整「关键决策」表格（品牌、技术栈、右栏定位、数据来源、节奏）。
2. 我按确认后的方案，创建具体任务并进入 **Phase 0 + Phase 1**（布局外壳）。
3. 每完成一个 Phase，我会给你演示/截图并请求反馈，再进入下一阶段。

---

*计划基于对 `Agent Web App - 三栏布局.pdf` 的视觉分析，以及对 `openx/app/serve/`、`openx/orchestration/`、`docs/openx-serve.md` 的代码阅读整理而成。*
